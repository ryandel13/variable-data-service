server:
  port: 8801

spring:
  application:
    name: variable-data-service
  cloud:
    config:
      discovery:
        enabled: true
      username: user
      password: password

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka/
 healthcheck:
   enabled: true